version: '3.8'

services:
  app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - ./gameon.db:/app/gameon.db
    environment:
      - NODE_ENV=development
    command: pnpm dev
